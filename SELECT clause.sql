SELECT * FROM list;

SELECT name, age FROM list;

SELECT * FROM list
    WHERE name != 'Paavan';

SELECT * FROM list
    WHERE name IN ('Subash', 'Ram', 'Hammy');

SELECT * FROM list
    WHERE age BETWEEN 18 AND 60;
